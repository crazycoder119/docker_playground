server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/crazycoder119/spring_docker_Kubernetes_cloud_config_repo
          clone-on-start: true